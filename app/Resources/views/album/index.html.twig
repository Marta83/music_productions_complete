<ul>
  {% for album in albums %}
    <li>
      <a id="album-{{album.id}}" class="album" href={{ path('delete_album', {'id' : album.id}) }}>Delete</a>
      <a id="add-artist-{{album.id}}" class="add-artist" href={{ path('add_artist', {'albumid' : album.id}) }}>Add artist</a>
      On the {{album.getPublishedAt|date("F jS")}} we published "{{album.title}}", with contributions by:

      <ol>
        {% for artist in album.getArtists %}
          <li>
            {{artist.getName}}, {{artist.getSpeciality}}
          </li>
        {% endfor %}
      </ol>
    </li>
  {% endfor %}
</ul>
